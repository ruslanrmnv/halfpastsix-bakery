// The one filled button. Ember has exactly one job on any page — this —
// so the class lives in one place and the phase-5 script can count fills.
export const ctaClass =
  "inline-flex min-h-14 cursor-pointer items-center justify-center rounded-full border-0 bg-ember px-8 font-display text-[16.5px] font-extrabold text-cocoa hover:bg-ember-deep";
